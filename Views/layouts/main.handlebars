<<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">
    <h1>
      Tech Blog
    </h1>
    <ul class="nav">
        <li>Home</li>
        {{#if logged_in}}
    </ul>
    {{#if logged_in}}
    <nav>
        <button class="addBtn" id="add post">Add a Post</button>
      <button class="outBtn" id="logout">Logout</button>
      {{else}}
      <button class="inBtn" id="login">Login</button>
      <button class="suBtn" id="signup">Sign Up</button>
    </nav>
    {{/if}}
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
